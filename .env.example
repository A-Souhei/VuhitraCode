# Required — enables the @secret agent
OLLAMA_MODEL=llama3.2

# Optional — defaults to http://localhost:11434
OLLAMA_URL=http://localhost:11434

# Optional — context window size in tokens (default 8192).
# Set this to match your Ollama model's num_ctx setting to avoid infinite compaction.
# Example: llama3.2 supports up to 131072; set both here and in the model's Modelfile.
OLLAMA_CONTEXT_SIZE=32768

# Optional — set to false if your model outputs tool calls as plain text instead of using
# the function-calling API (common with some quantized models). When false, gitignored files
# are faked (values redacted) instead of being delegated to the @secret agent. (default: true)
# OLLAMA_TOOLCALL=true

# Optional — maximum time (in minutes) for the review agent to complete a review (default: 30)
# REVIEW_MAX_TIME=30

# Semantic code indexing (Qdrant + Ollama embedding model)
# Also requires enabling in .vuhitra/settings.json: { "indexing": { "enabled": true } }
# QDRANT_URL=http://localhost:6333
# QDRANT_API_KEY=
# EMBEDDING_URL=http://localhost:11434
# EMBEDDING_MODEL=nomic-embed-text:latest
